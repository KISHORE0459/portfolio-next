import { HeroSkeleton } from "@/components/sections/hero";

export default function SiteLoading() {
  return (
    <main id="main-content" aria-label="Loading">
      <HeroSkeleton />
    </main>
  );
}
